# Antigravity 動作ルール（セキュリティポリシー）

このプロジェクトは**顧客情報を扱う業務用途**で使用されています。
以下のルールは絶対的な制約であり、いかなる理由があっても**必ず遵守**してください。

---

## 🚫 絶対禁止事項

### 1. 機密ファイルの操作・参照の完全禁止
以下の拡張子を持つファイルに対して、**削除・移動・名前変更・リダイレクト（`>`, `>>`）による上書きを一切禁じます**。
- `.pdf`, `.xlsx`, `.xls`, `.csv`, `.docx`, `.doc`, `.pptx`, `.ppt`
※ファイル名が日本語・英語・記号を問わず、該当する全ファイルが対象です。

### 2. 個人情報の出力禁止
以下の情報をコード、ログ、コメント、コンソール出力に**含めることを固く禁じます**。発見した場合は直ちにマスキング（例: `***`）してください。
- 氏名、住所、電話番号、メールアドレス
- マイナンバー、パスポート番号、在留カード番号、ビザ情報
- クレジットカード情報、銀行口座情報
- その他個人を特定できるあらゆる情報

### 3. 破壊的・変更系コマンドの実行禁止
以下のコマンド、および同等の結果をもたらす処理の実行を**禁止**します。
- `rm`, `del`, `erase`, `format`, `mv` (既存ファイルの上書きを伴う場合)
- ファイル内容を消去・上書きするリダイレクト操作（`> file` など）
- `git clean`, `git reset`, `git push --force`

---

## ✅ 必須の安全手順と実行ロック

### 処理の強制一時停止（WAIT_FOR_USER）
以下の操作を行う前は、計画をテキストで出力した上で**必ずプロセスの実行を一時停止し、ユーザーからの「承認キーワード」の入力を待機**してください。AI側の自己判断での進行は許可しません。
1. HTML/CSS/JS ファイルの編集・保存
2. 既存ファイルに対する上書き保存（同名の `.png` 画像の生成を含む）
3. 新規ファイルの作成
4. シェルコマンド（npm install 等）の実行

### Git操作の制限
- コミット前に必ず `git diff --staged` を実行し、変更内容をユーザーに提示して承認を待機すること。
- 機密情報が含まれている疑いがある場合は、操作を中止し警告を出力すること。

---

## 📁 ファイル構成のホワイトリスト（許可制）

**以下の「編集可能なファイル」以外への書き込みアクセスは一切禁止します。**

### 編集可能なファイル（明示的に許可されたもののみ）
- `index.html` / `nexio.html`
- `style.css` / `nexio-style.css`
- `script.js` / `nexio-script.js`
- `*.png` （※新規作成のみ許可。既存ファイルと同名での出力は禁止）
- `.gitignore`
- `GEMINI.md` / `SECURITY.md`