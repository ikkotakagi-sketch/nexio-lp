# セキュリティガイドライン

**最終更新日**: 2026年3月3日

このドキュメントは、本プロジェクトにおけるセキュリティルールを定義します。
すべての開発者・AI アシスタントはこのガイドラインに従ってください。

---

## 1. 機密ファイルの取り扱い

### 対象ファイル
| 種別 | 拡張子 | 例 |
|------|--------|----|
| 見積書・契約書 | `.pdf` | `2025見積書.pdf` |
| 顧客リスト | `.xlsx`, `.csv` | 顧客管理表 |
| 提案書・報告書 | `.docx`, `.pptx` | 業務提案書 |
| 認証情報 | `.env`, `.key`, `.pem` | APIキー、証明書 |

### ルール
- 機密ファイルはGitリポジトリにコミット**しないこと**
- 機密ファイルは暗号化ストレージまたはアクセス制限付きの場所に保管すること
- 不要になった機密ファイルは安全に削除（シュレッダー削除）すること

---

## 2. Git コミット前チェックリスト

コミットを行う前に、以下を必ず確認してください：

- [ ] `git diff --staged` で差分を確認した
- [ ] 個人情報（氏名、住所、電話番号等）が含まれていない
- [ ] APIキー、パスワード等の認証情報が含まれていない
- [ ] PDF/Excel/CSV 等の業務ファイルが含まれていない
- [ ] `.gitignore` が最新の状態である
- [ ] コミットメッセージに機密情報が含まれていない

---

## 3. Antigravity（AI アシスタント）利用時の注意

### やってはいけないこと
- AIに顧客の個人情報を入力すること
- AIに機密文書の内容を読み取らせること
- AIの提案するコマンドを確認せずに実行すること

### 安全な利用方法
- コマンド実行前に内容を確認する（`yolo` モードは使用しない）
- 機密データはダミーデータに置き換えてからAIに相談する
- AIが生成したコードに機密情報が含まれていないか確認する

---

## 4. インシデント発生時の対応

### 機密情報がGitにコミットされた場合
1. **即座に** `git reset` でコミットを取り消す
2. リモートにpush済みの場合は `git push --force` で履歴を上書き
3. GitHub/GitLab等のサービス側でもキャッシュをクリア
4. 漏洩した認証情報（APIキー等）は即座にローテーション（再発行）
5. 関係者に報告

### 重要ファイルを誤って削除した場合
1. `git checkout` で復元を試みる（Git管理下のファイルの場合）
2. OneDriveのバージョン履歴から復元を試みる
3. ごみ箱を確認する
4. バックアップから復元する

---

## 5. 定期的な確認事項

- [ ] `.gitignore` に新しい機密ファイルパターンが追加されているか
- [ ] 不要な機密ファイルがプロジェクトディレクトリに残っていないか
- [ ] AIアシスタントの設定（`yolo: false`）が維持されているか
- [ ] チームメンバーがセキュリティガイドラインを理解しているか
